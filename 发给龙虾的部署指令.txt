请你在这台服务器上执行「馨光 AI 设计灯光安装指导」部署流程，完成 OpenClaw 更新与 Miloco 2.0 部署。请严格按下面步骤执行，不要自行搜索网络、不要换安装路线、不要尝试源码安装、pip/pipx 安装、Docker 安装或手工解包安装。

目标：
1. 如果 OpenClaw 已预装，升级/配置 OpenClaw。
2. 如果 OpenClaw 未安装，自动安装 OpenClaw。
3. 安装并部署 Miloco 2.0。
4. 默认跳过微信绑定、米家账号绑定、MiMo API Key 配置。
5. 最后检查 OpenClaw gateway、Miloco 服务和插件状态。

唯一安装入口：
- 文件名：install-miloco-openclaw-cloud.sh
- 目标位置：/tmp/install-miloco-openclaw-cloud.sh
- 主下载地址：https://nijez.github.io/xingguang-ai-lighting-guide/install-miloco-openclaw-cloud.sh
- 备用下载地址 1：https://cdn.jsdelivr.net/gh/nijez/xingguang-ai-lighting-guide@main/install-miloco-openclaw-cloud.sh
- 备用下载地址 2：https://raw.githubusercontent.com/nijez/xingguang-ai-lighting-guide/main/install-miloco-openclaw-cloud.sh

执行要求：
1. 下载固定脚本：

```bash
curl -fsSL https://nijez.github.io/xingguang-ai-lighting-guide/install-miloco-openclaw-cloud.sh -o /tmp/install-miloco-openclaw-cloud.sh || curl -fsSL https://cdn.jsdelivr.net/gh/nijez/xingguang-ai-lighting-guide@main/install-miloco-openclaw-cloud.sh -o /tmp/install-miloco-openclaw-cloud.sh || curl -fsSL https://raw.githubusercontent.com/nijez/xingguang-ai-lighting-guide/main/install-miloco-openclaw-cloud.sh -o /tmp/install-miloco-openclaw-cloud.sh
chmod +x /tmp/install-miloco-openclaw-cloud.sh
```

2. 确认脚本是否存在、版本是否正确：

```bash
ls -l /tmp/install-miloco-openclaw-cloud.sh
grep '^SCRIPT_VERSION=' /tmp/install-miloco-openclaw-cloud.sh
```

3. 如果脚本不存在，或者版本低于 2026-06-23.9，请停止并告诉我：固定脚本下载失败。不要自己去网上找替代脚本。

4. 如果脚本存在且版本正确，后台启动安装，把所有输出写到固定日志文件，并按进程观察进度。观察期间每 20 秒汇报一次当前状态，最多观察 60 次；如果安装还没结束，不要中断进程，只告诉我安装仍在后台运行：

```bash
LOG=/tmp/openclaw-miloco-install-current.log
PIDFILE=/tmp/openclaw-miloco-install.pid
rm -f "$LOG"
nohup bash -lc 'INSTALL_ACTION=full INSTALL_NONINTERACTIVE=1 bash /tmp/install-miloco-openclaw-cloud.sh || INSTALL_ACTION=repair RUN_SYSTEM_UPGRADE=0 INSTALL_NONINTERACTIVE=1 bash /tmp/install-miloco-openclaw-cloud.sh' > "$LOG" 2>&1 &
echo $! > "$PIDFILE"
PID="$(cat "$PIDFILE")"
echo "安装进程已启动 PID=$PID 日志=$LOG"

for i in $(seq 1 60); do
  echo
  echo "===== 安装进度检查 $i/60 ====="
  if kill -0 "$PID" 2>/dev/null; then
    echo "状态: RUNNING"
  else
    echo "状态: FINISHED"
  fi

  grep -E 'Step [0-9]+/6|✓ Step|ERROR|Failed|Traceback|Timing:|All done|Done' "$LOG" 2>/dev/null | tail -n 30 || true
  echo "--- 最近日志 ---"
  tail -n 35 "$LOG" 2>/dev/null || true

  if ! kill -0 "$PID" 2>/dev/null; then
    break
  fi
  sleep 20
done

if kill -0 "$PID" 2>/dev/null; then
  echo "安装仍在后台运行，稍后可发送查看安装进度指令继续查看。"
fi
```

5. 安装期间不要中途杀进程，不要并行启动第二个安装，不要切换到其他安装方法。如果对话窗口没有实时显示输出，安装也仍在后台运行；之后如果我要看进度，我会发送“查看安装进度”的指令，你只需要读取 `/tmp/openclaw-miloco-install-current.log`。

6. 完成后执行验证：

```bash
miloco-cli service status
openclaw gateway status
openclaw plugins list | grep -i -C 2 miloco
ss -ltnp | grep -E ':(1810|18789)\b'
```

7. 最终只按下面格式回复我：

```text
馨光 AI 设计灯光部署结果

脚本版本：
OpenClaw 版本：
Miloco 状态：
Miloco 端口：
OpenClaw gateway 状态：
Miloco 插件状态：
总耗时：
日志文件：

下一步需要人工处理：
1. 配置 MiMo API Key：miloco-cli config set model.omni.api_key sk-你的Key
2. 绑定米家账号：miloco-cli account bind
3. 确认馨光淡彩光设备已出现在设备列表：miloco-cli device list
4. 如需个人微信/企业微信/飞书绑定，重新运行脚本进入平台绑定菜单。
```

重要限制：
- 不要把 Miloco 面板直接暴露到公网。
- `127.0.0.1:1810` 是云服务器内部地址，不能在用户自己的电脑上直接打开。
- 如果确实需要访问面板，只能先保持 SSH 隧道运行：ssh -L 1810:127.0.0.1:1810 用户名@服务器IP，然后本机浏览器再打开 http://127.0.0.1:1810/
- 不要输出或保存 SSH 密码、token、MiMo API Key。
